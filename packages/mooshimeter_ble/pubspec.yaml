name: mooshimeter_ble
description: Mooshimeter BLE transport and configuration-tree protocol.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  archive: ^4.0.7
  flutter:
    sdk: flutter
  universal_ble: ^2.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
